{
  "dependencies": {
    "@fullcalendar/daygrid": "^6.1.19",
    "@fullcalendar/react": "^6.1.19",
    "bcrypt": "^6.0.0",
    "cors": "^2.8.5",
    "dotenv": "^17.2.2",
    "express": "^5.1.0",
    "mongodb": "^6.20.0",
    "multer": "^2.0.2",
    "nodemailer": "^7.0.6",
    "socket.io": "^4.8.1"
  },
  "scripts": {
    "start": "node MongoDB/server.js",
    "dev": "nodemon MongoDB/server.js",
    "build-frontend": "cd sad-project && npm install && npm run build",
    "build": "npm run build-frontend && rm -rf MongoDB/public && cp -r sad-project/dist MongoDB/public"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}